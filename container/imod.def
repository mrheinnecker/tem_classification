## NOT A DEF FILE!!!!!!!!

# IMOD cntainer can be buold by following command (load imod module beforehand)

## build singularity image from module in EMBL HPC (load required modules beforehand)
bash /g/easybuild/scripts/generate_container_from_loaded_modules.sh -a rheinnec

