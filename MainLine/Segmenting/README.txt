microsamsegmenting - uses microsam on each tile and saves the masks for ulterior use.

maskmerge - merges the different tiles to create the final segmentation mask. Opens the mask tiles created by microsamsegmenting and saves a single merged mask.

maskclean - functions used by maskmerge to post process the masks for cleaner results.